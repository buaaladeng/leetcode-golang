func lengthOfLastWord(s string) int {
	n :=len(s)
	res := 0
	word := false
	for i:= n-1;i>=0;i--{
		if s[i] == ' '{
			if !word{
				continue
			}else {
				break
			}
		}else {
			word = true
			res++
		}
	}
	return res
}

