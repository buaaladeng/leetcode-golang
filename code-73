func setZeroes(matrix [][]int)  {
	m := len(matrix)
	n:= len(matrix[0])
	row := make([]int,0)
	clo:=make([]int,0)
	for i:=0;i<m;i++{
		for j:=0;j<n;j++{
			if matrix[i][j] == 0{
				row = append(row,i)
				clo = append(clo,j)
			}
		}
	}
	for i:=0;i<len(row);i++{
		tmp := row[i]
		for j:=0;j<n;j++{
			matrix[tmp][j] = 0
		}
	}

	for i:=0;i<len(clo);i++{
		tmp := clo[i]
		for j:=0;j<m;j++{
			matrix[j][tmp] = 0
		}
	}
}
