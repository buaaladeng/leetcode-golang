func maxSubArray(nums []int) int {
	n:= len(nums)
	res := nums[0]
	sum := 0
	for i:=0;i<n;i++{
		if sum >= 0{
			sum += nums[i]
		}else {
			sum = nums[i]
		}
		res = maxInt(res,sum)
	}
	return res
}
func maxInt(a,b int)int{
	if a>b{
		return a
	}else {
		return b
	}
}
