func isAnagram(s string, t string) bool {
	letters := make([]byte,128)
	for i:=0;i<len(s);i++{
		letters[s[i]]++
	}
	for i:=0;i<len(t);i++{
		letters[t[i]]--
	}
	for i:=0;i<len(letters);i++{
		if letters[i]!=0{
			return false
		}
	}
	return true
}
