func missingNumber(nums []int) int {
	n := len(nums)
	sum := 0
	for i:=0;i<n;i++{
		sum += i+1 - nums[i]
	}
	return sum
}

func missingNumber(nums []int) int {
	n := len(nums)
	for i:=0;i<len(nums);i++{
		n ^= i ^ nums[i]
	}
	return n
}

