func wordBreak(s string, wordDict []string) bool {
	n := len(s)
	dp := make([]bool,n+1)
	dp[0]=true
	for i:=1;i<=len(s);i++{
		for j:=0;j<i;j++{
			ok := false
			for _,word :=range wordDict{
				if s[j:i] == word{
					ok = true
					break
				}
			}
			if ok && dp[j]{
				dp[i] = true
				break
			}
		}
	}
	return dp[n]
}
