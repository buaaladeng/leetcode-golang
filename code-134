func canCompleteCircuit(gas []int, cost []int) int {
	start := 0
	own := 0
	remain := 0
	for i,g:=range gas{
		remain += g - cost[i]
		if remain<0{
			start = i+1
			own += remain
			remain = 0
		}
	}
	if remain + own <0 {
		return -1
	}
	return start
}
