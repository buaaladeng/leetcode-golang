func rotateRight(head *ListNode, k int) *ListNode {
	if head == nil || head.Next == nil{
		return head
	}
	res := new(ListNode)
	res.Next = head
	fast := res
	slow := res
	num := 0
	//cur := head
	for fast.Next!=nil{
		fast = fast.Next
		num++
	}
	k = k % num
	for num - k >0{
		slow = slow.Next
        k++
	}
	fast.Next = res.Next
	res.Next = slow.Next
	slow.Next = nil
	
	return res.Next
}

