func addDigits(num int) int {
	if num < 10{
		return num
	}
	res := 0
	for num > 9{
		for num > 0 {
			one := num%10
			res += one
			num /= 10
		}
		num = res
        res = 0
	}
	return num
}

func addDigits(n int) int {
	return (n-1)%9 + 1
}
