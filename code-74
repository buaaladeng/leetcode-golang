func searchMatrix(nums [][]int,target int)bool{
	n := len(nums)
	m := len(nums[0])
	i:= 0
	j:= m-1
	for i < n && j >= 0 {
		if nums[i][j]  == target{
			return true
		}else if nums[i][j] < target{
			i++
		}else {
			j--
		}
	}
	return false

}
