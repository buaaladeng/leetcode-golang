func reverse(x int) int {
	if x == 0{
		return x
	}
	neg := 1
	res := 0
	if x < 0{
		neg = -1
		x = -x
	}
	for x > 0 {
        res *= 10
		res += (x%10)
		x/=10
	}
	res *= neg
	if res > math.MaxInt32 || res < math.MinInt32{
		return 0
	}
	return res
}
